namespace Promethean.Core
{
    public class LevelTiler
    {
        public void TileLevel(byte[,] level)
        {
            for (var row = 0; row < level.GetLength(0); row++)
            {
                for (var column = 0; column < level.GetLength(1); row++)
                {

                }
            }
        }


    }
}